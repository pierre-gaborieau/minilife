import 'package:flutter/material.dart';

class SpecialCareers extends StatelessWidget {
  const SpecialCareers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text("SpecialCareers"),
    );
  }
}
